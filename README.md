## What is CyanEngine?

CyanEngine is not actually a "game" engine. It is a library/framework for creating games!

## What is in "CyanEngine"?

The main things that are included are: A custom launching page(to users specifc) and aa complex error logging system!

## Push and cloning

git commit -m "first commit"

git branch -M main

git push -u origin main

git remote add origin https://github.com/DarkSc0r/CyanEngine.git

git push -u origin main